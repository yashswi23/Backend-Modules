import chalk from "chalk";
import fs from 'fs';

fs.readFile('hello.txt','utf-8',(err,data)=>{
    console.log(chalk.green(data.slice(0,8)));
    console.log(chalk.red(data.charAt(7)));
    console.log(chalk.blue(data.split(",")));
})
